const fs = require('fs');

// Firebase is not available, we need to find another way.
// The raw DB data might be in the user's localStorage or we can just fetch it?
// We don't have access to Firestore.
